import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  list: {
    paddingBottom: 15,
    // backgroundColor: 'red',
  },
  title: {
    fontSize: 20,
    fontWeight: '500',
    paddingBottom: 5,
  },
});
export default styles;
